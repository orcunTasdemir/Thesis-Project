#class MyModel(tf.keras.Model):

# def __init__(self):
# super(MyModel, self).__init__()
# self.dense1 = tf.keras.layers.Dense(4, activation=tf.nn.relu)
# self.dense2 = tf.keras.layers.Dense(5, activation=tf.nn.softmax)

#num_of_mutations = 5


# cant move if the square in front of us is already occupied
                # then x and y stay the same at the end of the day only the facing direction
                # changed
                square_to_move = squares[agent.facing_direction]
                if not isinstance(square_to_move, Agent):
                    # now that moving is definitive, we can move out of our last spot
                    game.move_agent_out(agent_x, agent_y)
                    if isinstance(square_to_move, Food):
                        #if so, I want to add energy to my agent
                        agent.energy += square_to_move.energy
                        game.delete_food(agent_xx, agent_yy)